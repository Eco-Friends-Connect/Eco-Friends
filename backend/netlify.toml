[build]
  functions = "src/api"  # Directory where your serverless functions are located
  command = "npm install"  # Command to install dependencies

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
